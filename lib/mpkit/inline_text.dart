import 'package:flutter/widgets.dart';

class MPInlineText extends StatelessWidget {
  final Widget child;
  MPInlineText(this.child);

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
